<div class="container mx-auto my-8 px-4">
    <p class="text-lg font-semibold">Name:</p>
  <p class="leading-normal"><%= @tag.name %></p>

    <p class="text-lg font-semibold">Active:</p>
  <p class="leading-normal"><%= @tag.active %></p>

    <p class="text-lg font-semibold">User:</p>
  <p class="leading-normal"><%= @tag.user %></p>

    <div class="flex items-center justify-start mt-6">
    <%= link_to 'Edit', edit_tag_path(@tag), class: "btn btn-default mr-4" %>
    <%= link_to 'Back', tags_path, class: "btn btn-default" %>
  </div>
</div>