<div class="container mx-auto my-8 px-4">
    <p class="text-lg font-semibold">Name:</p>
  <p class="leading-normal"><%= @account.name %></p>

    <p class="text-lg font-semibold">User:</p>
  <p class="leading-normal"><%= @account.user %></p>

    <p class="text-lg font-semibold">Active:</p>
  <p class="leading-normal"><%= @account.active %></p>

    <p class="text-lg font-semibold">Investment:</p>
  <p class="leading-normal"><%= @account.investment %></p>

    <div class="flex items-center justify-start mt-6">
    <%= link_to 'Edit', edit_account_path(@account), class: "btn btn-default mr-4" %>
    <%= link_to 'Back', accounts_path, class: "btn btn-default" %>
  </div>
</div>