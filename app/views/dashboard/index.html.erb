<div class="px-4 py-4">
    <h1>Dashboard#index</h1>
    <p>Find me in app/views/dashboard/index.html.erb</p>

    <div class="py-6">
        <% if current_user %>
            <%= "Olá, #{current_user.email}" %>
        <% else %>
            
        <% end %>
    </div>
</div>